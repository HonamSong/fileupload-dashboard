# Runtime data: uploaded blobs + SQLite DB (contains user/API-key data — never commit)
data/
data_backup/
*.db
*.db-wal
*.db-shm

# Build artifacts
src/dashboard
src/fileupload-dashboard
dashboard
*.exe

# Local env / secrets
.env
.env.*
*.pem
*.key

# Per-environment compose (copy from docker-compose.sample.yml). Sample is committed.
docker-compose.yml
docker-compose.override.yml

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs / temp
*.log
tmp/
